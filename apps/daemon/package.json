{
  "name": "@agentskiss/daemon",
  "version": "0.1.0",
  "private": true,
  "description": "agentsKISS daemon — orchestration backend (placeholder, implemented in a later issue)",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agentskiss/shared": "workspace:*"
  }
}
