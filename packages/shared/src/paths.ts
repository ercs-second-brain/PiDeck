/** The port the daemon's HTTP surface and the CLI default to. */
export const DEFAULT_PORT = 8321;

/** Name of the per-user state directory under $HOME (the PD_HOME default). */
export const DEFAULT_STATE_DIR = ".pideck";