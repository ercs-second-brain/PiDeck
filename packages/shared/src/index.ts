/**
 * @agentskiss/shared — placeholder module.
 *
 * Real shared types/utilities (issue & PR models, daemon/web message contracts)
 * land in later issues. This export only proves the workspace builds and tests.
 */

export const PACKAGE_NAME = "@agentskiss/shared";

/** Placeholder identity helper, replaced by real shared code in later issues. */
export function placeholder(): string {
  return `${PACKAGE_NAME} placeholder`;
}
