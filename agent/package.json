{
  "name": "@agentskiss/agent",
  "version": "0.1.0",
  "private": true,
  "description": "pi coding agent integration — skills, extensions, prompts (placeholder, implemented in a later issue)",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
