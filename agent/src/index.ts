/**
 * @agentskiss/agent — placeholder module.
 *
 * The pi coding agent integration (orchestrator/worker prompts, custom pi
 * skills and extensions, tmux session wiring) is implemented in a later
 * issue. This module only proves the agent workspace package builds.
 */

export const AGENT_PACKAGE = "@agentskiss/agent";

/** Placeholder for the future orchestrator/worker prompt registry. */
export function placeholder(): string {
  return `${AGENT_PACKAGE} placeholder`;
}
