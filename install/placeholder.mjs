// Placeholder for the future install pipeline (auth onboarding, gh setup,
// repo connection, service registration). Replaced in a later issue.
console.log("@agentskiss/install placeholder: nothing to install yet");
