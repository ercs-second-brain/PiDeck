# install/

One-line install and service setup for agentsKISS (auth onboarding, gh CLI
setup, repo connection, launchd/systemd/WSL service registration) lands here in
a later issue. Currently a build-only placeholder.
